import { FilterLanguagesPipe } from './filter-languages.pipe';

describe('FilterLanguagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLanguagesPipe();
    expect(pipe).toBeTruthy();
  });
});
